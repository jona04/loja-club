/**
 * Placeholder home page for the storefront scaffold (`P3-FE-01`).
 *
 * Host resolution and the real templates (home/category/product) land in
 * `P3-SF-02`; this only needs to boot green.
 *
 * @returns A minimal placeholder page.
 */
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-semibold">Loja Club — storefront</h1>
    </main>
  )
}
